# Alignment Register (stub)

The **canonical** Alignment Register lives in **eggzWebApp** (`receipt-tracker-master`):

`docs/workspaces/cross-project/docs/alignment-register.md`

Open that file before changing help MDX, `docs.json` brand tokens, or any user-visible copy that should match the web app. If you find drift, add a row there — do not resolve by guessing.

**Source of truth:** design and product behaviour = web app (`lib/brand-tokens.ts`, UI routes, `autumn.config.ts`).

**This repo:** customer-facing Mintlify content under `help/`; agent workspaces under `docs/workspaces/`.
