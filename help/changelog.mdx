---
title: "Changelog"
description: "Release notes for the Help Centre, the eggz web app, and the eggz Tab Manager Chrome extension, newest first."
sidebarTitle: "Changelog"
icon: "clock-rotate-left"
keywords: ["changelog", "release notes", "updates", "what's new", "eggz Tab Manager", "Unscrambled Tabs"]
---

Newest updates appear **first**. Each entry shows the **date** (or “earlier” for older extension builds where we did not record the ship day) and which product it applies to — **Help Docs**, **Web App**, and/or **Chrome Extension** — matching the tags on the card.

For product help, see [Getting started](/help/guides/onboarding) or [email support](mailto:support@eggz.ai).

---

<Update label="2026-09-13--buy-documents-in-the-chat" description="Web App: buy more documents in the chat where the run stopped, the next plan up or a pack at the price shown" tags={["Web App", "Chrome Extension"]}>

**13 September 2026** · **Web App**

- **Buy documents where the run stopped.** When a run stops for want of documents, the chat now shows two cards: the next plan up, and a pack of 50, 100, 500 or any number you type. Packs start at 50 documents and cost 50 cents each, 45 cents from 100, and 35 cents from 500. **Recommended** sits on the lowest price per document. One click charges the card already on file for the figure on the button, and the documents are added. The receipt then offers **auto top-up**, off until you turn it on, topping up whenever fewer than 5 are left and emailing you a receipt each time. The same cards are in the Chrome extension.

</Update>

<Update label="2026-09-08--uploads-25-mb-and-heic" description="Web App and Chrome Extension: files up to 25 MB, iPhone HEIC photos, and GIF" tags={["Web App", "Chrome Extension"]}>

**8 September 2026** · **Web App** · **Chrome Extension**

- **Files up to 25 MB.** Scans and long PDFs that used to be turned away now upload.
- **iPhone photos upload as they are.** A HEIC photo is read like any other receipt, with no conversion first.
- **GIF joins PDF, JPG, PNG and WebP.** The same applies to **Upload document** in the Chrome extension.

</Update>

<Update label="2026-09-05--eggheadz-panel-on-top-and-mentions" description="Web App: the eggheadz panel sits on top of the page instead of squeezing it, resizes and remembers your width, and @ names a supplier, a document or a month in your question" tags={["Web App"]}>

**5 September 2026** · **Web App**

- **The eggheadz panel sits on top of your page now.** Open it and your page keeps its full width. Nothing behind it is dimmed, so you can click, scroll and type on the page while the answer is on screen.
- **Resize it.** Drag the left edge anywhere from 320 to 640 pixels. It settles on 360, 400 and 480 as you pass them, double-click puts it back to 400, and the arrow keys move it 40 pixels at a time. The width you chose comes back next time, for you and not for the computer.
- **Esc closes it from inside.** On a phone or a window under 1024 pixels wide it still slides in and covers the page.
- **Type @ to name the thing you are asking about.** Suppliers, customers, bills, invoices, quotes, receipts and months. Pick one and it becomes a chip in your question, and the answer is about that record. See [Asking eggheadz](/help/guides/asking-eggheadz).

</Update>

<Update label="2026-09-03--version-1-7-0" description="Chrome Extension: version 1.7.0, the eggheadz panel, one conversation with the web app, the bills already open in your tabs, Stop, and Chrome tab groups you can edit and drag" tags={["Chrome Extension"]}>

**3 September 2026** · **Chrome Extension**

**The eggheadz are in the panel now, and they know what you were doing.** Open the panel and you are in the same conversation you had on the web app, with the same Chats list, the same work waiting on you, and the same short list of things to ask. Ask here, open eggz in a tab, and you carry on where you stopped.

- **Ask from anywhere.** Command+J on a Mac, Ctrl+Shift+J on Windows and Linux, opens Ask eggheadz over whatever page you are on. The Ask tab shows the key that is bound right now, and you can change it at chrome://extensions/shortcuts.
- **It opens on what you can do.** Two example questions to tap, the jobs waiting on you with a count beside each, and, on a page it recognises, the job that page is about, offered before you ask.
- **Chats, kept.** Every past chat is in the Chats list, newest first, with its name written for you from your first exchange. Rename one, delete one, or start a New chat to change the subject.
- **On eggz.ai, the Ask eggheadz button opens this panel**, so there is one place to ask from whichever side you are on.
- **A document eggz keeps shows up as a card** in the chat, with the fields it read, so you can see what was filed without leaving the conversation.

**The bills already open in your tabs.** The invoices and receipts sitting in the tabs you opened this morning are the easiest to forget. eggz now finds them for you and lists them in the panel, biggest first. Tap one and it takes you to that tab, where the usual offer appears and names how many it found. Only counts ever leave a page: no page text, no links, nothing uploaded, and looking costs nothing. Switch it off any time under **Look for documents in open tabs** in Settings.

**Changed your mind mid-answer? Stop it.** The reply ends where it is, says *Reply stopped here.*, and stops spending. Asking again is never refused.

**Chrome tab groups, from inside eggz.**

- Your tab groups are in the Open Tabs list under their own name and colour. Collapse one here and it collapses on the tab strip, and the other way round. It is the same group, not a copy.
- Rename a group, change its colour, break it up or close it from its heading.
- Pick a few tabs and put them in a new group without touching the tab strip. If one is pinned, eggz says Chrome will unpin it and asks first.
- Drag a group's heading to reorder it, or onto another window to move the whole group. Drag a tab into a group or out to a spot of its own. Groups drag between the side panel and the dashboard too.
- A saved workspace shows the groups its tabs were saved in, and the headings fold, so you can see how a client is arranged before you open it.

**Smaller things worth knowing:**

- Duplicate tabs are called what they are: the Settings row reads **Spot duplicate tabs**.
- The header's door into the panel says **Ask eggheadz**, the same words as on the web app.
- A failure in the chat reads like a person wrote it, and the eggheadz are on it.

</Update>

<Update label="2026-09-02--record-pages-ask-the-panel" description="Web App: customer and contact pages hand their chat over to the one chat, which already knows the record you're on" tags={["Web App"]}>

**2 September 2026** · **Web App**

- **The chat came off the record pages, and lost nothing.** Customer and contact pages no longer carry a chat card of their own. Click **Ask** in the header instead, on any page.
- **It already knows who you're looking at.** Open the chat from a customer or contact page and that record's name sits above the message box as **About Bright Lane Trading**, so *"what do they still owe us?"* is answered about that account without you naming it.
- **One click drops it.** Standing on a customer's page and want to ask about something else? Click the **×** beside the name. See [Ask about a customer or contact](/help/guides/customers-and-contacts#ask-about-a-customer-or-contact).

</Update>

<Update label="2026-09-02--one-conversation" description="Web App: one running conversation, a Chats list of everything you asked before, and names written for you" tags={["Web App"]}>

**2 September 2026** · **Web App**

- **One conversation that stays with you.** Your chat with eggz is one running conversation now, not a fresh start every visit. Close the page, come back tomorrow, and you carry on where you stopped.
- **Every past chat is kept, and you can find it again.** Open **Chats** to see them, newest first. **New chat** starts a clean one whenever you want to change the subject. Rename a chat you will want later, or delete one you are done with.
- **Chats name themselves.** A chat you have not named takes its name from your first exchange, so the list reads back in your own words instead of a column of untitled rows.
- **The Chrome extension is in the same conversation.** Open the panel and you carry on the chat you were in on the web app, and the other way round.

</Update>

<Update label="2026-08-28--version-1-6-0" description="Chrome Extension: version 1.6.0 — pick exactly which bills to collect, tick them on the page itself, and narrow a run to invoices or receipts" tags={["Chrome Extension", "Help Docs"]}>

**28 August 2026** · **Chrome Extension** · **Help Docs**

**You can now take some of the bills on a page instead of all of them.** A supplier's billing portal listing fifty-four invoices used to be one button: everything, or nothing. There are now three rungs on that ladder, and you only climb as far as you need.

- **Invoices only** and **Receipts only** are back, as chips on the approval card. Tap one and the card tells you what it means for this page, as *12 of the 54 are invoices*. The choice is applied on your own computer when you approve, and the assistant is never told which one you picked.
- **Pick documents** opens a list that starts **empty**, with **Select all** at the top. Taking three of fifty-four should cost three taps, not fifty-one. The card keeps count as you go: *Nothing picked yet - Approve gets all 54*, then *3 of the 54 picked*.
- **Anything eggz already holds is greyed out and says "Already in eggz"** in that list. It cannot be ticked, and **Select all** leaves it out, because fetching it again would be free anyway.

**On a Stripe billing portal, the picker also draws checkboxes on the page itself**, against the real rows, next to the dates and amounts you are already reading. Click one row, **Shift-click** another, and everything between them is selected — which is how you take "everything since June" without anybody having to read a date. The boxes and the card's list are one selection, so ticking a row moves the count on the card. Everywhere else the card's own list is the picker and does the same job.

**The approval card no longer names a price.** It reads *54 invoices and receipts on this page*, and where it can it adds what is actually new: *3 new, 51 you already have*. Cost is stated once, after the run, where it is true.

**Smaller things worth knowing:**

- **The toolbar button is an on/off switch, not a door switcher.** Whichever door is open, the button closes it. Nothing open, and it opens whichever one **Open as** names. On a page that cannot host a card it opens the panel rather than doing nothing. A keyboard shortcut now goes to the door that is already open instead of raising a second one. See [Two ways to open it](/help/unscrambled-tabs/dashboard-and-side-panel#the-toolbar-button).
- **The bill banner stays until you close it.** It used to hide itself after a few seconds, which meant it could vanish off the screen of somebody who was reading the very page it was about. The **✕** is the only thing that closes it now.
- **A waiting approval card survives more.** Switching between Workspaces, Search and the chat no longer loses it, and a finished collect now stays in the chat it belongs to instead of turning up in a new one.
- **Enter sends the message in the side-panel composer**, and a send that is refused says why.
- **A run that is stopped, or one that runs out of credits part way, reports honestly**: what uploaded, what did not and why, and that nothing was spent on the rest.

**New help page:** [Collect every bill on a page](/help/unscrambled-tabs/collect-bills) covers the offer, the three ways to narrow it, what a run costs, and what it will not do.

</Update>

<Update label="2026-08-23--collect-says-what-it-costs" description="Help Docs and Web App: one document credit per upload; Chrome Extension: collecting bills says what it costs, remembers your answer, and reports at scale" tags={["Help Docs", "Web App", "Chrome Extension"]}>

**23 August 2026** · **Help Docs** · **Web App** · **Chrome Extension**

**Collecting bills from a billing page now tells you the cost before you approve.** The approval card reads "N invoices and receipts on this page - one credit each". If the page adds more documents while the card is waiting, the card updates to the new number; the run never collects more than the number you approved.

**Your answer sticks for as long as you stay on that page.** Deny the card and it will not ask again until you leave that page. Switching panel views, starting a new chat or closing and reopening the side panel no longer loses a waiting card or asks you the same question twice; a page you have already collected is not offered again until you leave it.

**A finished collect reports at scale.** Anything that did not file is grouped by reason with a count ("188 - The download failed") instead of one line per document. If your document credits run out part way, the card says so once, tells you nothing was spent on the rest, and links straight to Billing to top up. The finished card also states what you approved, so the record is there after you reopen the panel.

**Billing wording corrected, in the app and here.** Each upload counts as one document credit, however many pages the file has; the old wording that counted each page separately was wrong. A document eggz already holds is free to send again from the same website when the extension collects it. The plan table on [Uploading documents](/help/guides/uploading-documents) now matches the plans you can actually buy.

</Update>

<Update label="2026-08-03--version-1-5-1" description="Chrome Extension: an eggz tab pinned in every window, and a floating card that stays with you" tags={["Chrome Extension"]}>

**3 August 2026** · **Chrome Extension**

**Two bigger things:**

- **An eggz tab now sits pinned at the front of every window.** Open a new window and it is already there, so you are never more than one click from your workspaces. Close it and it stays closed in that window — it will not spring back at you — and it returns next time you start Chrome. If you would rather not have it at all, there is a switch: **Settings → Tabs → Keep the eggz tab pinned**. See [Settings](/help/unscrambled-tabs/settings-privacy#the-main-list).
- **The floating card stays with you.** It used to disappear the moment you followed a link or changed tabs, which meant the card was only really usable if you sat still. Now it follows you: click through as many pages as you like, switch between tabs, and it is still there, on the same screen, with the same conversation and even a half-typed message you had not sent yet. See [The floating card](/help/unscrambled-tabs/dashboard-and-side-panel#the-floating-card).

**And a polish pass on what was already there:**

- **The sidebar tells you which workspace you are in.** When it is collapsed to icons it had no way of showing you — the one you were working in sat among the rest with only a small green dot to tell them apart. It now leads with it.
- **The floating card and the side panel read the same way** left to right, so you are not relearning the same row of buttons twice. The card ends in the close button, with the size controls tucked behind it.
- **The middle card size is gone.** Two sizes and dragging any edge covers it.
- **Double-click the eggz logo on the card** to tuck it away to the nearest side.
- **Fixed: the tucked-away mascot could hide behind the scrollbar.** On any page with a scrollbar, everything docked to the right was landing about 15 pixels too far over.
- **Fixed: the mascot is smaller when tucked away**, and no longer wears a blue ring after you drag the card to the edge.
- **Fixed: folders now close again while you drag.** Hunting for the right one used to leave a trail of open folders behind it, pushing the one you wanted off the screen. Dropping into a folder you had just opened also did nothing at all; it works now.
- **The side panel can hand itself to the card.** **Move to floating card** puts eggz back over the page at the size you last used, and **Maximise** does the same at full-window size — Chrome does not let any extension resize its own side panel, so this hands you a card rather than pretending otherwise. If the page cannot take one, the panel stays where it is rather than leaving you with nothing.
- **Fixed: the side panel said the product name twice**, one line under the other.
- **Fixed: a scrollbar in the empty chat box**, and three stray lines in the collapsed sidebar that did not line up with anything.

</Update>

<Update label="2026-08-02--version-1-5-0" description="Chrome Extension: wiping data clears your snapshots too, approvals stay off until you switch them on, and shortcuts name the key on your keyboard" tags={["Chrome Extension"]}>

**2 August 2026** · **Chrome Extension**

- **Wipe all data now clears everything.** It used to leave your session snapshots behind, which held the name and address of every tab in them. If you wipe, it all goes.
- **Approvals are off until you turn them on.** The inbox is a switch in Settings under your eggz account. While it is off, the extension never checks for approvals at all.
- **Every workspace shows in the right-click menu.** It used to stop at 25 without saying so.
- **Keyboard shortcuts show the key on your keyboard.** Option on a Mac, Alt on Windows, instead of making you work out which one is yours.
- **Push the floating card to the edge of the screen** and it tucks away as the eggz mascot. Click it to bring the card back exactly as it was.
- **The Resources list handles long lists properly**, and the buttons on each row can now be reached with the keyboard.
- **Fixed: a rename could be undone by itself.** Renaming a workspace at the wrong moment could quietly revert.
- **Fixed: a tab could go missing when moved.** Moving a tab while the extension was mid-save could lose it.

</Update>

<Update label="2026-08-01--eggz-tab-manager-rename" description="Chrome Extension: Unscrambled Tabs is now called eggz Tab Manager & Bill Capture" tags={["Chrome Extension", "Help Docs"]}>

**1 August 2026** · **Chrome Extension** · **Help Docs**

The extension has a new name. **Unscrambled Tabs** is now **eggz Tab Manager & Bill Capture**. In ordinary writing we shorten it to **eggz Tab Manager**.

The old name only ever described the tab half. It said nothing about sending a bill you have on screen straight into your eggz account, which is the other half of what the extension does. The new name says both.

Nothing about how it works has changed, and there is nothing for you to do:

- Your workspaces, saved tabs, custom tab names, snapshots and settings are exactly where you left them. No reinstall, no re-signing in.
- Backups you exported under the old name still restore.
- The right-click menu on any web page now reads **eggz Tab Manager**. The three items under it are unchanged: **Rename this tab…**, **Save for later & close tab**, and **Move tab to workspace**.
- Help Centre addresses have not moved, so anything you bookmarked still works.
- Entries further down this page keep the old name, because that is what the product was called on the day each one shipped.

</Update>

<Update label="2026-08-01--card-door-drag-and-command-bar" description="Chrome Extension: the toolbar button now opens a floating card over the page, dragging tabs gained copy and undo, right-click works on any workspace row, ⌘K runs actions by name, and Settings has been reorganised" tags={["Chrome Extension"]}>

**1 August 2026** · **Chrome Extension**

- **Two ways to open it.** The toolbar button now opens a floating card over the page you're reading. Nothing on the page moves, and you can drag the card anywhere or resize it. If you prefer the docked side panel, **Settings → Open as** switches between them and takes effect on your next click. See [Two ways to open it](/help/unscrambled-tabs/dashboard-and-side-panel).
- **Dragging tabs got a lot clearer.** A card follows your cursor for the whole drag and says exactly what letting go will do, as in *"Moving 1 tab to Marchmont Cafe — hold ⌥ Option to copy"*. It names the workspace you're over and spells the key the way your own keyboard does. Move is the default everywhere. Hold **⌥ Option** (**Alt** on Windows and Linux) as you let go to copy instead. See [Moving tabs](/help/unscrambled-tabs/moving-tabs).
- **Undo any drop.** Every drop leaves a message with an **Undo** button for ten seconds, or press **⌘Z** / **Ctrl+Z**. Tabs an undone move had closed come back with their back-button history intact where Chrome still remembers them, rather than reloading from scratch. See [Undo](/help/unscrambled-tabs/moving-tabs#undo).
- **Right-click any workspace row.** It opens the same menu as the row's **⋯** button, right where your cursor is, in the card, the side panel and the dashboard sidebar. See [The row menu](/help/unscrambled-tabs/workspaces#row-menu).
- **Archive a workspace instead of deleting it.** Archiving takes a workspace out of every list into a collapsed **Archived** section at the bottom, keeping its tabs, links and names. It also comes out of search and bulk actions, so nothing sweeps it up by accident. **Restore** puts it back. See [Archiving](/help/unscrambled-tabs/workspaces#archiving).
- **⌘K is now a command bar.** On the dashboard it still finds your workspaces, tabs and saved links, and it now runs actions by name too: create a workspace, switch, archive, save or move this tab, suspend other tabs, close duplicates, toggle the sidebar, open settings. Commands only appear when they'd actually do something. See [The command bar](/help/unscrambled-tabs/ai-and-search#the-command-bar).
- **Settings, reorganised.** The main list is now the handful of things people actually change. **Protected domains**, **Performance & RAM**, **Keyboard shortcuts** and **Sessions** live behind one **Advanced** row. Every screen's back button names where it goes, and the shortcut list shows the keys you have bound right now rather than the factory defaults. See [Settings & privacy](/help/unscrambled-tabs/settings-privacy).
- **New help pages.** [Getting started](/help/unscrambled-tabs/getting-started) walks a brand-new user from install to their first moved tab, and [Troubleshooting](/help/unscrambled-tabs/troubleshooting) covers what people actually run into.

</Update>

<Update label="2026-07-30--tab-management-wave" description="Chrome Extension — switching brings everything back exactly, tabs get your own names, one-motion moves, one-click duplicate clean-up, and per-tab memory controls" tags={["Chrome Extension"]}>

**30 July 2026** · **Chrome Extension**

- **Switching now brings *everything* back.** Leave a workspace and return to find your **tab groups** (names, colours, even which ones were collapsed), **pinned tabs**, and **the exact tab you were on** — all exactly as you left them. Restarting Chrome restores the same detail. See [Switching — everything comes back](/help/unscrambled-tabs/workspaces#switching).
- **Give any tab your own name.** Two tabs on the same page for different clients? Right-click the page → **Unscrambled Tabs** → **Rename this tab…** (or rename from any tab list). Your name shows on the tab strip itself, sticks through switches and restarts, and renamed-apart tabs are never mistaken for duplicates. See [Rename any tab](/help/unscrambled-tabs/manage-tabs#rename-any-tab).
- **Move a tab to another workspace in one motion.** Drag it onto a workspace in the dashboard sidebar, or right-click the page → **Move tab to workspace** — your most-recent workspaces are listed first. The tab is saved there and closed here. **Alt+D** saves a copy without closing. See [Move a tab](/help/unscrambled-tabs/manage-tabs#move-a-tab-to-another-workspace).
- **Save for later & close tab.** Right-click any page to file it into your workspace's saved links and close it — clutter gone, page kept.
- **Close duplicates in one click.** A **Close duplicates** button appears in the [Open Tabs view](/help/unscrambled-tabs/open-tabs#close-duplicates-in-one-click) whenever you've got the same page open more than once — it keeps the copy you're using (pinned tabs and renamed tabs are always safe) and closes the rest, across every window.
- **Suspend exactly the tabs you mean.** Put one tab to sleep from its row, or **Suspend others** to quieten a whole window's background tabs — memory freed, tabs stay on the strip, protected sites always skipped. See [Suspend a tab](/help/unscrambled-tabs/manage-tabs#suspend-a-tab).
- **Search got bigger — and came to the dashboard.** Press **/** (or ⌘K / Ctrl+K) on the dashboard for a search palette, and search everywhere now spans your **open tabs** and **saved tabs** (custom names included) as well as workspaces and resources — Enter jumps straight to the result. See [Search anything](/help/unscrambled-tabs/ai-and-search#search-anything-k).
- **Flip between clients from the keyboard.** **Alt+A** opens the workspace quick switcher, most-recent first — and tapping **Alt+A then Enter** bounces you straight back to your previous workspace. See [Keyboard shortcuts](/help/unscrambled-tabs/keyboard-shortcuts).

</Update>

<Update label="2026-07-14--same-purchase-and-final-say" description="Web App — two documents for one purchase get combined and counted once, you can override any classification, and eggz flags weak GST paperwork before audit time" tags={["Web App"]}>

**14 July 2026** · **Web App**

- **Two documents, one purchase.** An invoice and its payment receipt are the same purchase, and eggz now treats them that way: clear matches are **combined** automatically and counted once, with both files kept and a one-click **Separate** to undo. Borderline matches become a **"Looks like the same purchase"** card showing both documents and what they share, and you answer **Yes, same purchase** or **No, keep separate**. This works across sources too, so a receipt you photograph can match an invoice that arrived by email. See [Two documents, one purchase](/help/guides/receipts-and-bills#two-documents-one-purchase).
- **Proof of payment arrived.** When a receipt matches an invoice still marked unpaid, the card says so, and combining keeps both files, marks the invoice paid, and counts the purchase once. A receipt attached to a bill now reads **Proof of payment** in its Process panel, and **Separate from bill** replaces the old Unlink wording.
- **Purchases to review, on your dashboard.** Admins see pending same-purchase suggestions as a card on the dashboard home, so a couple of taps clears them. The card only appears when there's something to review. See [Purchases to review](/help/guides/dashboard#purchases-to-review).
- **You always have the final say.** eggz's classification is an editable default, never a verdict. The receipt page adds **This is actually a bill** (with an Undo) and a one-click paid ⇄ unpaid switch. When a change has a real consequence, such as a possible double-count or a paid-status conflict, an orange note explains it first; it advises and never blocks. See [You always have the final say](/help/guides/receipts-and-bills#you-always-have-the-final-say).
- **Weak GST paperwork gets flagged early.** A purchase over A$82.50 whose document is missing an ABN or a GST breakdown may not substantiate a GST credit at audit. eggz now shows a calm standing note on such receipts so you can ask the supplier for a proper tax invoice while it's still easy. See [The GST substantiation note](/help/guides/receipts-and-bills#the-gst-substantiation-note).
- **Pick a currency when you create documents.** Invoice, bill, quote, and customer forms now have a **Currency** picker, defaulting to your reporting currency, and line items and totals follow the currency you choose.
- **Smaller fixes.** Your **Billed / Collected / Outstanding** choice now sticks for the browser session; voided documents consistently show **$0 due**; and the mobile menu's **Home** and **Approvals** items now open their pages.

</Update>

<Update label="2026-07-11--detail-pages-related-timeline-mobile" description="Web App — every document detail page now links its related records, shows an activity timeline, and works properly on your phone; accepted quotes convert to invoices in one click" tags={["Web App"]}>

**11 July 2026** · **Web App**

- **See what's connected, one click away.** Invoice pages now show a **Customer** card; bill pages show a **Supplier** card and one **Evidence receipt** card per attached receipt. See [Related records](/help/guides/invoices-and-quotes#related-records).
- **Convert an accepted quote straight to an invoice.** An **Accepted** quote you created in eggz gets a **Convert to invoice** button — confirm, and eggz creates a linked **Draft** invoice with zero effect on your reports until you authorise it. The quote becomes **Invoiced**. See [Converting a quote to an invoice](/help/guides/invoices-and-quotes#converting-a-quote-to-an-invoice).
- **See what's happened to a document.** Invoice, bill, receipt, quote, and customer pages can all show an **Activity** timeline of system events — things like **Voided**, **Draft created**, or **Converted to an invoice** — newest first, with a chip for anything done by AI or automatically. See [Activity](/help/guides/invoices-and-quotes#activity).
- **Customer and supplier pages lead with what's owed.** The account page header now shows the **open balance** — red, and naming the overdue amount, when something's overdue — and its related documents sort unpaid and most-overdue first. See [Open balance](/help/guides/customers-and-contacts#open-balance).
- **Properly usable on your phone.** Document detail pages now show a **Details / View document** toggle and keep the page's actions pinned to the bottom of the screen. See [Viewing on your phone](/help/guides/invoices-and-quotes#viewing-on-your-phone).

</Update>

<Update label="2026-07-11--email-in-body-fallback" description="Web App — no attachment? eggz now reads the email itself instead of quarantining it" tags={["Web App"]}>

**11 July 2026** · **Web App**

- **No attachment, no problem.** A forwarded email with no usable attachment but readable body content — a typical SaaS billing email — now becomes a **Pending** receipt from its message body instead of being quarantined. It's only quarantined when there's truly nothing to read, labelled **Invalid attachment or no readable content**. See [Emails with no attachment](/help/guides/email-in#emails-with-no-attachment).

</Update>

<Update label="2026-07-10--email-in-receipt-forwarding" description="Web App — forward receipts and bills to your workspace's own email address" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **Email your receipts straight in.** Every workspace now has its own private email address (claim yours in **Settings > Integrations**). Forward a receipt or bill to it — from your phone, your inbox, or a supplier — and each attachment of a processed email becomes a **Pending** receipt with AI extraction, ready for your review. Unknown senders are held in **quarantine** for an admin to release, and nothing is published to Xero without your approval. See [Email-in: forward receipts to eggz](/help/guides/email-in).

</Update>

<Update label="2026-07-10--receipt-resolution-panel" description="Web App — every receipt now shows its stage at a glance, with one clear next step and GST broken out" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **Every receipt shows where it stands.** The receipt page now has a **Process** panel that names the stage — Reading, Couldn't read, Needs review, Needs a decision, Expense, Attached to bill, or Published to Xero — and when there's a step to take, the top of the page offers exactly one: **Resolve** (attach to a bill or create an expense), **Add a date**, or **View bill**. An Expense-stage receipt publishes from the **Publish to Xero** card instead, so it has no top button. See [The Process panel](/help/guides/receipts-and-bills#the-process-panel).
- **GST is first class.** When a receipt carries tax, the figures read **Total (incl. GST) · GST · Subtotal (excl. GST)** — the split AU substantiation needs — and the supplier links straight through to its account.
- **Unlinking is safe.** Unlinking a receipt from a bill sends it back to **Needs a decision** rather than quietly turning it into a fresh expense.

</Update>

<Update label="2026-07-10--invoice-lifecycle-and-settlement" description="Web App — void or delete invoices properly, reconcile edited amounts automatically, flag receipts with no date, and see full payment history on invoices and bills" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **Void and delete invoices properly.** Draft invoices can now be deleted outright. Issued invoices are **voided** instead — kept forever for your audit trail, but excluded from reports and tax totals. If payments are already applied, eggz offers to **unapply them and void**: the payment money is never deleted, it becomes unallocated credit you can apply elsewhere. See [Voiding and deleting invoices](/help/guides/invoices-and-quotes#voiding-and-deleting-invoices).
- **Editing an amount can't desync your line items anymore.** Change a receipt's total and eggz either absorbs it into the single line item automatically (with an Undo), or opens a **Reconcile line items** dialog so the total and the lines always agree before you save. See [Editing a receipt's amount](/help/guides/expenses#editing-a-receipts-amount).
- **Receipts with no date get flagged, not guessed.** A **Needs date** receipt is excluded from Billed month totals and shown as **Undated** in Outstanding, with a one-tap bank-match suggestion or manual entry to fix it — and publishing to Xero is blocked until it's dated. See [Receipts without a date](/help/guides/receipt-dates).
- **See exactly how an invoice or bill was paid.** The detail page now shows a full **Settlement** history — Total, Paid, Amount due, and every payment, credit note, or prepayment applied, in order. Click a payment to see its full detail. See [Settlement history](/help/guides/invoices-and-quotes#settlement-history).

</Update>

<Update label="2026-07-10--xero-branding-and-publish" description="Web App — your Xero branding on every document, and publish eggz drafts straight to Xero" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **Your Xero branding, in eggz.** The invoice and quote **branding themes** you built in Xero now show in **Settings > Integrations**. Pick a default theme for documents you publish, override it per document, and use **View branded PDF** to see an invoice or quote exactly as Xero renders it — logo, layout, and colours. Included with the **Growth** and **Scale** plans. See [Your Xero branding in eggz](/help/guides/xero-branding).
- **Publish drafts straight to Xero.** Drafted an invoice or quote in eggz — including one from the AI assistant? Click **Publish draft to Xero** to create it as a **draft** in Xero: nothing is sent to your customer, Xero assigns the number, and you review, approve, and send it there. The document links to Xero, so future syncs update it instead of duplicating. See [Publish invoices & quotes to Xero](/help/guides/publish-to-xero).

</Update>

<Update label="2026-07-10--invite-only-founding-members" description="Web App — eggz is now invite-only, with a personal founding-member welcome from first receipt to first Xero publish" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **eggz is now invite-only.** We're onboarding founding members personally. If you don't have an invite, leave your details on the new [request-invite page](https://app.eggz.ai/request-invite) and we'll be in touch — the **Sign Up** button now takes you there while self-serve sign-up is paused.
- **A personal welcome for founding members.** Invited founders skip the setup wizard entirely: sign in with your invited email (no password to create — just pick a sign-in option like Google), and a guided welcome takes you from your **first receipt**, to **connecting Xero**, to **reviewing and publishing** that receipt into your books. See [Founding members: your first ten minutes](/help/guides/onboarding#founding-members-your-first-ten-minutes).
- **You always approve before anything reaches Xero.** eggz reads and prepares your documents, but nothing is written to your books without your explicit approval — during the welcome and everywhere else.
- **Not ready to finish setup?** A **Skip setup for now** link on every welcome step takes you straight to your dashboard; you can connect Xero and upload receipts anytime from **Settings > Integrations**.

</Update>

<Update label="2026-07-10--publish-receipts-to-xero" description="Web App — publish a processed receipt to Xero from its detail page: review the pre-filled details, approve, done" tags={["Web App"]}>

**10 July 2026** · **Web App**

- **Publish receipts to Xero.** Processed receipts now have a **Publish to Xero** card on their detail page. Click it, review the pre-filled details (contact, date, amount, line items), optionally pick an expense account — and for **paid** receipts, the **bank account** the money came from — then approve. **Unpaid** receipts become **bills**; **paid** receipts become **Spend Money** transactions. Nothing is written to Xero without your explicit approval, each receipt publishes once, and eggz remembers the link so the next Xero sync recognises the record instead of duplicating it. If Xero rejects a publish, you see the exact error and the receipt stays unpublished. See [Publish a receipt to Xero](/help/guides/expenses#publish-a-receipt-to-xero).

</Update>

<Update label="2026-07-09--dashboard-save-cancel-and-personal-views" description="Web App — Customize mode gets a real Save/Cancel, a personal default reporting view, and clearer expense drill-downs" tags={["Web App"]}>

**9 July 2026** · **Web App**

- **Customize mode now has Save and Cancel.** Nothing changes until you click **Save** — **Cancel**, or **Esc**, discards your edits, and a failed save keeps you in edit mode so nothing's lost. **Reset to default** only takes effect once you save it. New dashboards open with **Revenue, Monthly Spend, Expense Breakdown, Quote Pipeline**, left to right. See [Personalising your dashboard](/help/guides/personalising-dashboard).
- **Widgets resize in clean steps** — quarter, third, half, or full width, one or two units tall — so your layout always tiles neatly. While customizing, widgets are for dragging and resizing only; charts and links inside them aren't clickable until you save or cancel.
- **Pin your own default reporting view.** Click the pin next to the **Billed / Collected / Outstanding** toggle to make your current view your personal default — click it again to follow your organisation's default instead. It only changes what you see. See [Two things worth knowing](/help/guides/reporting-views#two-things-worth-knowing).
- **Clearer Expense Breakdown drill-downs.** Clicking a slice shows the records behind the number in your active view — including whole documents with **no line detail** — and the rows always add up to the card. See [Seeing what's behind a number](/help/guides/reporting-views#seeing-whats-behind-a-number).
- **Simpler chart taps on mobile.** Tapping a chart now shows a tooltip with a **Show More** link to the full report, instead of navigating straight there.

</Update>

<Update label="2026-07-08--lists-your-way-and-chart-drilldowns" description="Web App — customise any list's columns, and tap any dashboard chart to drill into the numbers" tags={["Web App"]}>

**8 July 2026** · **Web App**

- **Make every list your own.** Show, hide, and reorder the columns on any list — Receipts, Bills, Sales, Quotes, Customers, and Transactions — from the **Columns** button in the toolbar. Your layout is saved just for you and waiting next time. See [Customise your table columns](/help/guides/expenses#customise-your-table-columns).
- **Tap a chart, see the detail.** Click any dashboard card — or a month, bar, or category on it — to jump straight to the transactions behind the number, already filtered. Each card has a **?** that explains what it measures, and on your phone you can tap into **explore mode** to scrub exact values. Expense categories now read in plain English, too. See [Drill into the numbers](/help/guides/dashboard#drill-into-the-numbers).

</Update>

<Update label="2026-07-05--one-seamless-ai" description="Chrome Extension — replies stream in live, your chat survives a browser restart, and page-read permission is now one simple tap" tags={["Chrome Extension"]}>

**5 July 2026** · **Chrome Extension**

- **Replies stream in live.** The assistant now writes its answer as it goes, so you're never left watching a blank box.
- **Pick up where you left off.** Your conversation is saved to your account — close the browser, come back later, and it's still there. Your chat is private to your account, so signing in as a different account simply starts a fresh one.
- **Simpler page-read permission.** When the AI wants to read a page, it's now one clear choice — **Approve** or **Deny** — asked each time. See [AI assistant & the command bar](/help/unscrambled-tabs/ai-and-search#it-asks-before-it-reads-a-page).
- **The option to use your own Anthropic key is gone.** The chat now runs on the included eggz assistant only, so there is no key to paste. Signing in is what turns the assistant on. See [AI assistant & the command bar](/help/unscrambled-tabs/ai-and-search).

</Update>

<Update label="2026-06-17--direct-install-links" description="Help Docs — one-click Chrome Web Store install links throughout the Help Centre" tags={["Help Docs"]}>

**17 June 2026** · **Help Docs**

- **Install in one click.** The Help Centre now links straight to the **Unscrambled Tabs** [Chrome Web Store listing](https://chromewebstore.google.com/detail/unscrambled-tabs/gfjdjhpcemmakhoaggnihfcohkmfcfcc) instead of asking you to search the store. A **Get the Chrome Extension** link sits at the top of every page, and both the [Install guide](/help/unscrambled-tabs/install) and the [extension overview](/help/unscrambled-tabs/index) now open the listing directly.
- **Find it where you need it.** Added install links to the web-app guides that mention the extension — [Uploading documents](/help/guides/uploading-documents), [Expenses](/help/guides/expenses), [Capture bills in ChatGPT & Claude](/help/guides/ai-bill-capture), and [Getting started](/help/guides/onboarding).

</Update>

<Update label="2026-06-16--consent-reading-dashboard-pinned" description="Extension v1.3.0 — the AI asks before reading a page, a slick new dashboard sidebar with drag-and-drop, and pinned workspaces" tags={["Chrome Extension"]}>

**16 June 2026** · **Chrome Extension** · **v1.3.0**

- **The AI asks before it reads a page.** The chat sees your tabs' **names and addresses** by default — but to read **what's actually on a page** (or one of your records), it now pops up a permission card: **Allow once**, **Always allow** (for this conversation), or **Deny**. Nothing on a page leaves your browser until you say yes. See [AI assistant & the command bar](/help/unscrambled-tabs/ai-and-search#it-asks-before-it-reads-a-page).
- **Cleaner reading.** When you do approve, the extension pulls out the real article or content and skips menus, ads, and clutter — so the AI works from the good stuff, not the junk.
- **A slick new dashboard sidebar.** The dashboard now has a slim **icon rail**: hover a folder to fan out its workspaces, pin the full list open with **⌘\\ / Ctrl+\\**, and grab quick **Switch** / **Open in new window** actions on hover. Plus **drag-and-drop** to reorder folders, workspaces, and tabs (or drop a tab onto a workspace to move it). See [switching from the dashboard sidebar](/help/unscrambled-tabs/workspaces#switching).
- **Pinned workspaces.** Pin the workspaces you live in so they're always one click away at the top of the rail. See [Pin to sidebar in the row menu](/help/unscrambled-tabs/workspaces#row-menu).
- **Capture the original bill.** "Save to eggz" now sends the **actual PDF** — filename and all pages intact — instead of a screenshot, so multi-page invoices arrive complete. See [Capture bills from any page](/help/unscrambled-tabs/capture-bills).
- **A warmer welcome.** First-run setup now helps you **create your first workspace** from the tabs you already have open (and pins it) — no more empty screen.
- **Easier on the eyes.** Sharper text contrast and readable notice banners in both **light** and **dark** mode.

</Update>

<Update label="2026-06-15--unified-ai-and-open-tabs" description="Extension v1.2.0 — one smart AI chat (no more mode switch), AI that sees every window's tabs and your workspaces, and a new Open Tabs view" tags={["Chrome Extension"]}>

**15 June 2026** · **Chrome Extension** · **v1.2.0**

- **One chat, no switch.** The two separate AI helpers (and the little toggle between them) are gone. There's now a **single chat** that quietly picks the right helper: signed in with credits → the included eggz assistant; out of credits with your own key → your key as a **backup**; signed out with a key → your key. See [AI assistant & the command bar](/help/unscrambled-tabs/ai-and-search).
- **Your AI sees the whole browser.** The chat now knows about **every open tab in every window** (even sleeping/suspended ones, by title and address) *and* your **saved workspaces, folders, and links** — so one chat answers both your books and your tabs. Sleeping tabs stay asleep; it only reads their title and address.
- **New Open Tabs view.** See every tab across every window in one list, then **save the keepers** into a workspace. Choose the sort order and grouping, and pick where it lives via the **Placement** setting. See [Open Tabs view](/help/unscrambled-tabs/open-tabs) for how it works today.
- **Organise, everywhere.** **Organize** moved into the chat header and now tidies tabs across **all** windows, saving groups into new or existing workspaces.
- **Out of credits? You're not stuck.** A dismissible banner offers a one-click **Upgrade**, and if you have your own key the chat keeps going on it. No key? The upgrade card also lets you **invite a friend for free credits**.
- Plus a friendlier emoji picker for workspaces/folders and tidier in-app confirmation dialogs.

</Update>

<Update label="2026-06-14--xero-organisation-selection" description="Choose which Xero organisation to connect; disconnecting now revokes access at Xero" tags={["Help Docs", "Web App"]}>

**14 June 2026** · **Help Docs** · **Web App**

- **Choose your Xero organisation.** If your Xero login has access to more than one organisation, eggz now shows a selection screen after you authorise so you can pick which one to connect — single-organisation logins connect automatically. See [Xero integration](/help/guides/xero-sync).
- **Switch organisations anytime.** To change which organisation is linked, disconnect and reconnect, then choose the other one. Only one Xero organisation connects per workspace.
- **Disconnect now revokes access at Xero.** Disconnecting withdraws eggz's access on Xero's side immediately, rather than only removing the local connection. Your already-synced data stays in eggz.

</Update>

<Update label="2026-06-12--capture-bills-and-onboarding" description="Capture bills from any page, a first-run welcome, and a gentle bill-capture nudge" tags={["Chrome Extension"]}>

**12 June 2026** · **Chrome Extension**

- **Capture bills from any page.** Land on an invoice page or open a bill PDF and a banner offers to **send it straight to eggz** in one click — same upload pipeline and credits as the dashboard, so it lands in [Expenses](/help/guides/expenses) and starts extracting right away. The banner tidies itself away if you ignore it. See [Capture bills from any page](/help/unscrambled-tabs/capture-bills).
- **A friendly first run.** New installs get a short three-step welcome — **Welcome → Sign in to eggz → Done** — with signing in (AI included, no key) as the recommended path.
- **A gentle nudge.** Visited a few supplier or shop sites lately? The AI chat may show a *"capture any bills?"* suggestion chip with your message pre-filled. The list of recent sites stays **on your computer** and clears when you close Chrome.

</Update>

<Update label="2026-06-11--ai-assistant-included" description="AI assistant included with every plan — no API key needed when signed in" tags={["Web App", "Chrome Extension"]}>

**11 June 2026** · **Web App** · **Chrome Extension**

- **AI assistant included with every plan.** The CRM chat in the web app and the extension's Tabs AI (chat about tabs, **Organize**, save-resources) are covered by your eggz plan — fair use applies. You only pay for documents processed.
- **No API key needed in the extension.** Signed in to eggz, all Tabs AI features work without an Anthropic key. Your own key, if configured, keeps being used.
- If your organization is an unusually heavy AI user, the assistant shows an upgrade prompt instead of stopping silently.

</Update>

<Update label="2026-06-10--unscrambled-tabs-eggz-integration" description="Sign in to eggz from Unscrambled Tabs: shared login, document upload, CRM chat" tags={["Chrome Extension", "Web App"]}>

**10 June 2026** · **Web App** · **Chrome Extension**

- **One login, both surfaces.** [Connect your eggz account](/help/unscrambled-tabs/eggz-account) from the side panel — sign in on either the extension or the web app and both share the session; signing out anywhere signs you out everywhere.
- **Upload from the side panel.** Send a **PDF, JPEG, PNG, or WebP** (up to 10MB) or a **screenshot of the current tab** straight into your organization — same credits and extraction pipeline as the dashboard.
- **CRM chat in the extension.** Signed in, the AI tab adds your organization's **eggz CRM agent** (no API key needed): streamed answers about bills, invoices, quotes, accounts, and receipts, with your active tab as context. A header switcher flips between **eggz CRM** and the original **Tabs AI** (organise, tab sources, your own Anthropic key) — signing in never removes a feature. Signed out, the Tabs AI welcome screen offers **Sign in / Sign up** — see [AI assistant & the command bar](/help/unscrambled-tabs/ai-and-search).
- The agent can read documents you've just uploaded ("what's the status of the bill I just sent?").

</Update>

<Update label="2026-06-05--in-app-help-embed-fix" description="Help Centre loads inside the web app again" tags={["Help Docs", "Web App"]}>

**5 June 2026** · **Help Docs** · **Web App**

- The in-app **Help Centre** panel can embed help pages from `eggz.ai/help` without the browser showing “refused to connect”.
- Allowed embed origins: apex (`eggz.ai`), production app (`app.eggz.ai`), staging (`staging.app.eggz.ai`), preview (`preview.app.eggz.ai`), Vercel preview deployments (`*.vercel.app`), and local dev (`http://localhost:3000`).

</Update>

<Update label="2026-06-05--in-app-contextual-help" description="Get help without leaving the app" tags={["Web App"]}>

**5 June 2026** · **Web App**

- Open **Help Centre** from the nav or the **?** hint on key pages — guides load in a side panel so you stay in eggz.
- Covers dashboard upload, expenses, invoices, quotes, customers, contacts, settings, Xero integrations, and onboarding.

</Update>

<Update label="2026-06-05--onboarding-help-alignment" description="Onboarding guide: six steps, all supported regions, optional plan step" tags={["Help Docs"]}>

**5 June 2026** · **Help Docs**

- [Getting started](/help/guides/onboarding) now matches the app wizard: **six steps** including an optional **plan** step (**Continue on Free**).
- Region table lists **Australia, Canada, United States, United Kingdom, and New Zealand** with currency and date-format notes.
- Help Centre typography aligned to **Geist** (web app brand).
- Free tier: **50 document credits**, one-time (not monthly) — clarified on the plan step.

</Update>

<Update label="2026-06-01--mcp-bill-capture-mvp" description="Bill capture in ChatGPT & Claude — one-step save, no required reason field" tags={["Help Docs", "Web App"]}>

**1 June 2026** · **Help Docs** · **Web App**

- **Saving is now one step.** Review the card and **Save to eggz** — no required "reason" or extra accounts-payable fields. Only a supplier, currency and one line item are needed; everything else is optional.
- **Smoother sign-in on save** in Claude and ChatGPT — when you save as a guest, your assistant now prompts you to connect eggz inline and saves automatically once you're in.
- Added **troubleshooting** tips to the [bill capture guide](/help/guides/ai-bill-capture).

</Update>

<Update label="2026-05-31--mcp-bill-capture" description="Capture bills in ChatGPT & Claude (MCP) — extract, categorise, save to eggz" tags={["Help Docs", "Web App"]}>

**31 May 2026** · **Help Docs** · **Web App**

- **Capture bills inside ChatGPT and Claude.** Share a receipt or bill in your AI assistant and eggz extracts the supplier, line items, amounts and tax, **auto-categorises** each line, and shows an editable card. Review, then **Save to eggz** — one-tap sign-in if you're new — and the bill lands in **[Expenses](/help/guides/expenses)**.
- **No account needed to try it:** extraction runs as a guest preview; your data is saved only when you sign in.
- New guide: [Capture Bills in ChatGPT & Claude](/help/guides/ai-bill-capture).
- Rolling out via the ChatGPT app and Claude connector directories.

</Update>

<Update label="2026-05-09--help-url-migration" description="Help Centre now lives at eggz.ai/help · 301 from help.eggz.ai" tags={["Help Docs", "Chrome Extension"]}>

**9 May 2026** · **Help Docs** · **Chrome Extension**

- The Help Centre is now at **`eggz.ai/help`** (not **`help.eggz.ai`**). Old URLs are **301-redirected** path-for-path, e.g. `help.eggz.ai/unscrambled-tabs/install` → `eggz.ai/help/unscrambled-tabs/install`.
- **Why:** keep SEO and brand on the **`eggz.ai`** domain; one host for marketing, app, and docs.
- **How:** Mintlify hosts the docs; a **Cloudflare Worker** proxies **`/help/*`** to **`eggz.mintlify.app`**. Another Worker **301**s the legacy **`help.eggz.ai`** hostname to **`eggz.ai/help/*`**. Internal links and this changelog use **`/help/...`** paths throughout.
- **Chrome Extension:** **Performance & RAM → Learn more** opens [`/help/unscrambled-tabs/performance-and-ram#performance-modes-explained`](/help/unscrambled-tabs/performance-and-ram#performance-modes-explained).

</Update>

<Update label="2026-05-09--chrome-ext-v1-1-3" description="Chrome extension v1.1.3 — help links, icons, About version" tags={["Chrome Extension"]}>

**9 May 2026** · **Chrome Extension**

- **Performance & RAM → Learn more** points at this Help Centre (performance modes). Help base URL defaults to **`https://eggz.ai`** with docs under **`/help`**.
- **Settings > About → Version** reads the live **Manifest V3** version from Chrome; builds also embed the same semver for tests and non-extension contexts.
- **Icons:** toolbar and Web Store icons generated from one **master PNG** via **Sharp** (letterboxed on black). **`pnpm run build`** runs icon generation first and fails clearly if the asset is missing.
- **Tests** for version resolution with a stubbed Chrome API and outside the extension runtime.

</Update>

<Update label="2026-05-09--help-tabs-and-pages" description="Help Docs · top tabs (Web App / Chrome Extension) and eight extension pages" tags={["Help Docs", "Chrome Extension"]}>

**9 May 2026** · **Help Docs** · **Chrome Extension**

- **Navigation:** **Web App** and **Chrome Extension** are **top-level tabs**; extension articles sit under **Chrome Extension → Unscrambled Tabs**. **Resources → Changelog** stays under **Web App**.
- **New / updated extension articles:** [Overview](/help/unscrambled-tabs/index), [Install](/help/unscrambled-tabs/install), [Workspaces & tabs](/help/unscrambled-tabs/workspaces), [AI & search](/help/unscrambled-tabs/ai-and-search), [Session time travel](/help/unscrambled-tabs/session-time-travel), [Backup & import](/help/unscrambled-tabs/backup-import), [Settings & privacy](/help/unscrambled-tabs/settings-privacy), [Performance & RAM](/help/unscrambled-tabs/performance-and-ram) (including [`#performance-modes-explained`](/help/unscrambled-tabs/performance-and-ram#performance-modes-explained)).
- [Home](/help/index) highlights Unscrambled Tabs with quick links to the extension overview.

</Update>

<Update label="2026-05-04--help-seo-changelog-nav" description="Help Docs · guide SEO metadata and Changelog in the sidebar" tags={["Help Docs"]}>

**4 May 2026** · **Help Docs**

- Stronger **`title`** and **`description`** front matter on guides for clearer search snippets and previews.
- **Resources → Changelog** added to navigation so release notes sit beside the rest of the docs.

</Update>

<Update label="2026-04-27--web-app-quiet-week" description="Web App · no user-facing release this week" tags={["Web App"]}>

**27 April 2026** · **Web App**

No customer-visible changes this week. Engineering continued work on document upload, [Xero sync](/help/guides/xero-sync), and [dashboard analytics](/help/guides/dashboard). Follow this changelog for when those ship.

</Update>

<Update label="2026-04-21--ci-licence-guardrails" description="Web App · Chrome Extension · dependency and licence checks in CI" tags={["Web App", "Chrome Extension"]}>

**21 April 2026** · **Web App** · **Chrome Extension**

- **CI / engineering:** dependency and **licence allowlist** checks in build pipelines (including the extension repo). **No** change to dashboard or extension UI from this work alone.
- Roadmap: document upload, Xero sync, and analytics — watch this page and [Getting started](/help/guides/onboarding).

</Update>

<Update label="ext-v1-1-2" description="Chrome extension v1.1.2 · RAM modes, discard rules, multi-window binding" tags={["Chrome Extension"]}>

**Earlier (before 9 May 2026)** · **Chrome Extension**

- **Performance modes:** **Manual** (default), **Balanced**, or **Aggressive** — controls eligible inactive-tab discard via [`tabs.discard`](https://developer.chrome.com/docs/extensions/reference/api/tabs). **Free RAM now** discards safe inactive tabs in one shot; **multi-window** sweeps include all normal windows.
- **Workspace binding** per Chrome window so the side panel and dashboard follow the workspace for **that** window.
- **Safety:** never auto-discards active, pinned, audible, already discarded, `chrome://` / extension pages, non-http(s) URLs, or **Protected domains**.
- **`windows`** permission for reliable [`chrome.windows.getCurrent`](https://developer.chrome.com/docs/extensions/reference/api/windows) when routing and bindings.
- Internal RAM/performance QA checklist (aggregate metrics only); more tests for **discard eligibility**.

</Update>

<Update label="ext-v1-1-0" description="Chrome extension v1.1.0 · emoji favicons, side panel polish, themes, protected domains" tags={["Chrome Extension"]}>

**Earlier** · **Chrome Extension**

- **Custom emoji** for tabs missing favicons.
- **Side panel:** less flash on workspace switch; batched background tab updates; remembers scroll, view, and expanded workspaces.
- **Themes:** token-based light/dark with **WCAG AA**-oriented dark mode; light-mode toasts, tooltips, modals, and export/import flows follow the theme.
- **Protected domains** so important sites are not auto-suspended.
- **Stability:** safer behaviour with no browser window; fewer errors when restoring sessions.

</Update>

<Update label="ext-v1-0-0" description="Chrome extension v1.0.0 · first public release" tags={["Chrome Extension"]}>

**Earlier** · **Chrome Extension**

- Workspaces with tab grouping and organisation; AI tab search and assistant; **Session time travel** (snapshots every **5 minutes**); tab dedupe; tab suspension to free RAM; **AES-256-GCM** backup/restore; **Workona** import; shortcuts (**⌘K** search, **⌘J** assistant); dashboard with resizable panels.

</Update>

---

### For maintainers (entry format)

Use one `<Update>` per ship batch. **Entries describe what changed for the person reading, and nothing else.** No store submissions, no review timelines, no "on its way", no packaging or signing steps — release logistics are not a changelog entry. **`label`** — unique anchor, e.g. `YYYY-MM-DD--topic` or `ext-vMAJOR-MINOR-PATCH` when no calendar date exists. **`description`** — short headline for the timeline. **`tags`** — only **Help Docs**, **Web App**, and/or **Chrome Extension** (same order as the bold line: Help Docs → Web App → Chrome Extension). Put the **calendar date** and scopes on the first line inside the block, as in the entries above.
