---
title: "Installing Unscrambled Tabs"
description: "Install the Unscrambled Tabs Chrome extension from the Web Store, pin it for quick access, and optional unpacked load for developers"
icon: "download"
---

<Card title="Add Unscrambled Tabs to Chrome" icon="chrome" href="https://chromewebstore.google.com/detail/unscrambled-tabs/gfjdjhpcemmakhoaggnihfcohkmfcfcc" horizontal>
  Opens the official Chrome Web Store listing — click **Add to Chrome** and you're installed in seconds.
</Card>

## Install from the Chrome Web Store

1. Open the [**Unscrambled Tabs** listing on the Chrome Web Store](https://chromewebstore.google.com/detail/unscrambled-tabs/gfjdjhpcemmakhoaggnihfcohkmfcfcc).
2. Click **Add to Chrome** and confirm the permissions prompt.
3. After install, **pin the extension** so the toolbar icon is always visible:
   - Click the puzzle piece in the Chrome toolbar → find **Unscrambled Tabs** → click the pin icon.

<Tip>
Pinning matters because the **side panel**, **dashboard**, and **⌘K / Ctrl+Shift+K** flows are much easier to reach when the action is one click away.
</Tip>

## First open

After install, typical first steps:

1. Click the **Unscrambled Tabs** icon to open the **side panel** (primary UI).
2. The first time you open it, a short **welcome wizard** says hello: **Welcome → Sign in to eggz → Create your first workspace → Done**. Signing in to eggz is the recommended path — you get the AI included (no key) plus [bill capture](/help/unscrambled-tabs/capture-bills). The wizard then turns the tabs you already have open into your **first workspace** (and [pins it](/help/unscrambled-tabs/workspaces#pinned-workspaces) for you) — or, if you skip, it sets up a starter workspace so you're never staring at an empty screen.
3. Open **Settings** in the side panel to set your **Performance mode** and **Protected domains**, and to [connect your eggz account](/help/unscrambled-tabs/eggz-account) for the AI, uploads, and bill capture.

<Note>
Signing up for eggz from the extension? If your account doesn't have a workspace yet, we'll pop you over to the web to **finish setting up** — it takes about 60 seconds, then you're back in the side panel with the AI and bill capture unlocked.
</Note>

## Full-page dashboard (optional)

The extension can open a **full-page dashboard** for a larger workspace view. Use the controls described in your build (for example toolbar menu or in-product links). The dashboard uses the same data as the side panel.

## Keyboard shortcuts

| Action | Mac | Windows / Linux |
|--------|-----|------------------|
| Open search | ⌘K | Ctrl+Shift+K |
| Open AI assistant | ⌘J | Ctrl+Shift+J |

Shortcuts are defined in the extension manifest and can be customised in **Chrome → Extensions → Keyboard shortcuts**.

## Developers — load unpacked

If you are building from source:

1. Run your team’s build command (for example `pnpm run build`) to produce an unpacked `dist/` folder.
2. Chrome → **Extensions** → enable **Developer mode** → **Load unpacked** → select `dist/`.

<Warning>
Unpacked builds are for development only. End users should install from the **Chrome Web Store** so Chrome can apply automatic updates and signature checks.
</Warning>

## Permissions — why they exist

The extension needs broad **tab** access to save workspaces, switch tabs, capture snapshots, and use discard. **Your browsing data stays local** to the extension unless you explicitly use features that send content to **eggz** — the AI assistant, signed-in CRM chat, and document uploads. See [Settings & privacy](/help/unscrambled-tabs/settings-privacy) for detail.
